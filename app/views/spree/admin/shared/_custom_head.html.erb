<style>
  /* Gastos de Inventario - morado (admin) */
  .card.bg-purple,
  .card.bg-purple .card-body { background-color: #6f42c1 !important; color: #fff !important; }
  .badge-purple { background-color: #6f42c1; color: #fff; }

  /* Tarjetas reporte egresos: misma altura, bordes y dimensiones */
  .expense-report-cards .card { min-height: 140px; border-radius: 0.375rem; border: 1px solid rgba(0,0,0,0.125); }
  .expense-report-cards .card-body { padding: 1rem; }
  .expense-report-cards .card-title { font-size: 0.875rem; font-weight: 600; }
  .expense-report-cards .card-amount { font-size: 1.25rem; font-weight: 700; }
  .expense-report-cards .card small { font-size: 0.75rem; }
</style>
